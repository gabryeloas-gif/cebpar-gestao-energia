{
  "name": "cebpar-gestao-energia",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply cebpar-db --local",
    "db:migrate": "wrangler d1 migrations apply cebpar-db --remote",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.9.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.35.0"
  }
}
